# ============================================================
# 03 — 我的快餐店不可能破产！？（快餐店模拟经营 Demo）
# 项目级忽略规则。
# 根目录 (D:\shengqu) .gitignore 已统一排除：dist/ build/ node_modules/
# .workbuddy/ .codebuddy/ .memeskill/ *.log 等，此处不重复。
# ============================================================

# ---------- 覆盖根规则：本项目 assets/ 必须入库 ----------
# 根 .gitignore 全局排除了 assets/（那条规则针对 01 这类「运行时用
# public/config.json 里的 TOS URL 引用」的项目）。
# 但本项目源码 index.html 直接引用 assets/*.png —— 不入库的话 clone 下来
# 源码跑不起来。且这些美术是 AI 生成的，重新生成无法还原同一张。
# 必须先 !assets/ 重新纳入目录本身，再 !assets/** 纳入内容，
# 否则「父目录被排除时无法 re-include 子文件」。
!assets/
!assets/**

# ---------- 构建产物（dist/index.html 可由 _build_dist.py 重建）----------
dist/

# ---------- CDP 冒烟截图 / 视觉验证截图 ----------
_cap_*.png
_sheet*.png

# ---------- 一次性调试脚本与抓取产物 ----------
# 约定：以下划线开头的脚本/产物一律视为 scratch，不入版本库；
# 有长期价值的工具在下面用 ! 显式放行。
# 注意：gitignore 不支持行尾注释（会把 "# xxx" 当成文件名的一部分），
# 注释必须单独占一行。
_*
# —— 放行清单（必须放在通配之后）——
# 冒烟测试：24 用例回归
!_smoke2.cjs
# 分层体系验证 + 真实拖拽交付端到端验证
!_check_layers.cjs
# 多店铺层级/顾客遮挡度量
!_cap_layer_shops.cjs
# 发布构建：内含 32 个资源的 TOS URL 映射，是 assets 丢失后的恢复依据
!_build_dist.py
# 门面贴花生成器：产出 facade_1~9.png
!_facade.py
# 封面/预览合成
!_preview.py
